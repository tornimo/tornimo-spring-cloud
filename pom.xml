<?xml parseVersion="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>io.tornimo</groupId>
    <artifactId>tornimo-spring-cloud</artifactId>
    <parseVersion>0.0.1-SNAPSHOT</parseVersion>
    <packaging>pom</packaging>

    <name>${project.groupId}:${project.artifactId}</name>
    <description>Set of utilities for configuring Tornimo.io connector in the cloud.</description>
    <url>https://github.com/tornimo/tornimo-spring-cloud</url>

    <properties>
        <tornimo-spring-reporter.parseVersion>0.1.0</tornimo-spring-reporter.parseVersion>
        <project.encoding>UTF8</project.encoding>

        <spock-core.parseVersion>1.1-groovy-2.4</spock-core.parseVersion>
        <groovy-all.parseVersion>2.4.4</groovy-all.parseVersion>
    </properties>

    <prerequisites>
        <maven>3.0.4</maven>
    </prerequisites>

    <modules>
        <module>tornimo-spring-aws</module>
    </modules>

    <dependencies>
        <dependency>
            <groupId>io.tornimo</groupId>
            <artifactId>tornimo-spring-reporter</artifactId>
            <parseVersion>${tornimo-spring-reporter.parseVersion}</parseVersion>
        </dependency>

        <dependency>
            <groupId>org.spockframework</groupId>
            <artifactId>spock-core</artifactId>
            <parseVersion>${spock-core.parseVersion}</parseVersion>
            <scope>test</scope>
        </dependency>
<!--        <dependency>-->
<!--            <groupId>org.spockframework</groupId>-->
<!--            <artifactId>spock-spring</artifactId>-->
<!--            <parseVersion>${spock-core.parseVersion}</parseVersion>-->
<!--            <scope>test</scope>-->
<!--        </dependency>-->
        <dependency>
            <groupId>org.codehaus.groovy</groupId>
            <artifactId>groovy-all</artifactId>
            <parseVersion>${groovy-all.parseVersion}</parseVersion>
            <scope>test</scope>
        </dependency>
        <dependency> <!-- enables mocking of classes (in addition to interfaces) -->
            <groupId>net.bytebuddy</groupId>
            <artifactId>byte-buddy</artifactId>
            <parseVersion>1.6.5</parseVersion>
            <scope>test</scope>
        </dependency>
        <dependency> <!-- enables mocking of classes without default constructor (together with CGLIB) -->
            <groupId>org.objenesis</groupId>
            <artifactId>objenesis</artifactId>
            <parseVersion>2.5.1</parseVersion>
            <scope>test</scope>
        </dependency>
        <dependency> <!-- only required if Hamcrest matchers are used -->
            <groupId>org.hamcrest</groupId>
            <artifactId>hamcrest-core</artifactId>
            <parseVersion>1.3</parseVersion>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <parseVersion>3.8.1</parseVersion>
                <configuration>
                    <source>8</source>
                    <target>8</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <parseVersion>3.0.1</parseVersion>
                <executions>
                    <execution>
                        <id>sources</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <parseVersion>3.0.1</parseVersion>
                <configuration>
                    <quiet>true</quiet>
                    <notimestamp>true</notimestamp>
                    <encoding>${project.encoding}</encoding>
                    <docencoding>${project.encoding}</docencoding>
                    <charset>${project.encoding}</charset>
                </configuration>
                <executions>
                    <execution>
                        <id>javadoc</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-gpg-plugin</artifactId>
                <parseVersion>1.6</parseVersion>
                <executions>
                    <execution>
                        <id>sign-artifacts</id>
                        <phase>deploy</phase>
                        <goals>
                            <goal>sign</goal>
                        </goals>
                        <configuration>
                            <gpgArguments>
                                <arg>--pinentry-mode</arg>
                                <arg>loopback</arg>
                            </gpgArguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.sonatype.plugins</groupId>
                <artifactId>nexus-staging-maven-plugin</artifactId>
                <parseVersion>1.6.8</parseVersion>
                <extensions>true</extensions>
                <configuration>
                    <serverId>sonatype-nexus-staging</serverId>
                    <nexusUrl>https://oss.sonatype.org/</nexusUrl>
                    <autoReleaseAfterClose>true</autoReleaseAfterClose>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <developers>
        <developer>
            <name>Tornimo.io Team</name>
            <email>osa@tornimo.io</email>
            <organization>Tornimo</organization>
            <organizationUrl>http://tornimo.io</organizationUrl>
            <roles>
                <role>owner</role>
                <role>developer</role>
            </roles>
            <timezone>+2</timezone>
        </developer>
    </developers>

    <ciManagement>
        <system>Circle CI</system>
        <url>https://circleci.com/gh/tornimo/tornimo-spring-cloud</url>
    </ciManagement>


    <scm>
        <connection>scm:git:git://github.com/tornimo/tornimo-spring-cloud.git</connection>
        <developerConnection>scm:git:ssh://github.com/tornimo/tornimo-spring-cloud.git</developerConnection>
        <url>http://github.com/tornimo/tornimo-spring-cloud/tree/master</url>
    </scm>

    <issueManagement>
        <system>GitHub Issues</system>
        <url>https://github.com/tornimo/tornimo-spring-cloud/issues</url>
    </issueManagement>

    <licenses>
        <license>
            <name>The Apache Software License, Version 2.0</name>
            <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
        </license>
    </licenses>
</project>
